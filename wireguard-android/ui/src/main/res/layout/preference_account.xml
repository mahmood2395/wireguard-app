<?xml version="1.0" encoding="utf-8"?><!--
  ~ Copyright © 2026 Portway. All Rights Reserved.
  ~ SPDX-License-Identifier: Apache-2.0
  ~
  ~ Layout for AccountPreference. It deliberately declares none of androidx-preference's
  ~ well-known ids (@android:id/title and friends): this row draws itself entirely, and leaving
  ~ those ids out is what stops Preference.onBindViewHolder writing a title over the card.
  -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="22dp"
    android:layout_marginTop="6dp"
    android:layout_marginBottom="10dp"
    android:background="@drawable/account_card_bg"
    android:orientation="vertical"
    android:paddingHorizontal="16dp"
    android:paddingVertical="15dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:baselineAligned="false"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/account_pref_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:ellipsize="end"
            android:maxLines="1"
            android:textAppearance="?attr/textAppearanceTitleSmall"
            android:textColor="?attr/colorOnSurface"
            android:textSize="15sp" />

        <TextView
            android:id="@+id/account_pref_plan"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:textAppearance="?attr/textAppearanceBodySmall"
            android:textColor="@color/accent_300"
            android:textSize="12.5sp" />
    </LinearLayout>

    <!-- The plan rail. Shown only when the panel states a quota; a bar needs a denominator and
         inventing one would be the single most actionable lie on the screen. -->
    <ProgressBar
        android:id="@+id/account_pref_rail"
        android:mirrorForRtl="true"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="4dp"
        android:layout_marginTop="12dp"
        android:max="1000"
        android:progressDrawable="@drawable/account_rail"
        android:visibility="gone" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="9dp"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/account_pref_usage"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:fontFeatureSettings="tnum"
            android:textAppearance="?attr/textAppearanceBodySmall"
            android:textColor="@color/clay_text_muted"
            android:textSize="12sp" />

        <TextView
            android:id="@+id/account_pref_days"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFeatureSettings="tnum"
            android:textAppearance="?attr/textAppearanceBodySmall"
            android:textColor="@color/clay_text_muted"
            android:textSize="12sp" />
    </LinearLayout>
</LinearLayout>
