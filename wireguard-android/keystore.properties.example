# Copy to keystore.properties (git-ignored) and fill in. Release signing is skipped
# entirely when this file is absent, so a plain clone still builds unsigned.
storeFile=/Users/m.mansouri/dev/wireguard-app/keystore/portway-release.jks
storePassword=placeholder-change-me
keyAlias=portway
keyPassword=placeholder-change-me
